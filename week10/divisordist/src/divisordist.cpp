#include <iostream>
#include <vector>

using namespace std;

typedef vector<int> vi;

int main() {
    int t; cin >> t;

    for(int i=0; i<t; ++i) {
        int n, c; cin >> n >> c;

        vi divisors;
        divisors.reserve(n);

        for(int j=0; j<c; ++j) {
            int u, v; cin >> u >> v;

        }
    }
    return 0;
}
